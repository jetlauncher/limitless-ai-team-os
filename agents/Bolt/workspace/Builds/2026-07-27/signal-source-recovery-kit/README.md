# Signal Source Recovery Kit v0

## Why this exists
Recurring blocker: Signal/X intel is degraded because recent notes report `x_search` HTTP 400, `xurl` HTTP 402, and Qwen seeing only X interface boilerplate.

## Open
- Start with `index.html` in this folder.
- Machine-readable checklist: `data.json`.

## Safe morning flow
1. Check credential/billing status without printing secrets.
2. Run one manual source fetch after any fix.
3. If blocked, use alternate public AI sources for the day.
4. Record the result in `Agents/Shared Memory/Daily/2026-07-27.md`.

## Safety
Local artifact only. No billing changes, token exposure, deploys, cron edits, or external messages.
