# Nightly Agent Sync Console — 2026-07-18

Open `index.html` in a browser. It shows the file-only sync status for every active local Hermes profile with an Obsidian agent folder.

Files:
- `index.html` — phone-friendly local dashboard
- `agent-sync-status.json` — reusable status data

Verification: generated with 13 agents and linked from Shared Memory daily.
