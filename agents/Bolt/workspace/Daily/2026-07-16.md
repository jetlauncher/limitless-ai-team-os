## 2026-07-16 — YouTube Blog Daily Check

**Action:** Ran `youtube_blog_daily_check.py` for @jeditrinupab latest videos.

**Result:**
- New article generated: video `6AD6OBtawZA` → slug `20-leadership-skills-ai-era-2026`
- Title: "20 ทักษะผู้นำที่ชนะในยุค AI ฉบับ 2026"
- Transcript source: YouTube captions (Thai)
- 4 other latest videos already in blog (skipped)
- Total articles: 202

**Key issue found:** Gemini key in `blog-engine.cjs` (legacy fallback) is INVALID (HTTP 400 API_KEY_INVALID). Valid key located at `/Users/ultrafriday/.config/friday/gemini_api_key.env`. Ran script with `GEMINI_API_KEY` env var from that file.

**Deploy:** Committed to main branch in `~/.hermes/exports/limitless-club-website-main-youtube`, pushed to origin/main, deployed to Vercel prod. Verified live at jeditrinupab.com.

**Repo state note:** The primary `/clawd/builds/limitless-club-website` repo has a corrupted `.git/index` (Resource deadlock avoided / mmap failed). Used the clean worktree at `~/.hermes/exports/limitless-club-website-main-youtube` instead. Untracked orphan JSON files exist in the exports dir from previous duplicate slug runs — not committed.

**Next action:** Update the script's Gemini key fallback to read from `/Users/ultrafriday/.config/friday/gemini_api_key.env` instead of the stale blog-engine.cjs, or set `GEMINI_API_KEY` in the cron environment.
