# Bolt Daily — 2026-06-30

## YouTube Blog Daily Check
- Script found 1 new video: `5Yq_LMCUJpI` — "[Full Course] สร้าง Personal Brand บน YouTube ด้วย AI"
- 4 other latest videos already in blog (skipped).
- **Blocker resolved**: Gemini key in `blog-engine.cjs` was invalid (HTTP 400). Used valid key from `~/.config/friday/gemini_api_key.env` via `GEMINI_API_KEY` env var.
- **iCloud deadlock**: clawd builds repo had `Resource deadlock avoided` on git operations. Used clean clone from `origin/main` in `/tmp/blog-deploy/` per task instructions.
- Article generated, build passed (`npm run build`), committed and pushed to `main`, deployed to Vercel production.
- Production verified: article at top of `articles.json` (196 total), browser-rendered correctly at `jeditrinupab.com/blog/สร้าง-personal-brand-youtube-ai`.

## Key fix needed
- `youtube_blog_daily_check.py` `get_gemini_key()` legacy fallback reads from `blog-engine.cjs` which has a stale/invalid key. The valid key is in `~/.config/friday/gemini_api_key.env`. Should add this path to the fallback chain.
