# 2026-07-19 — Bolt Daily

## Hermes Agent Update Check (Cron Run)

### Version Status
- **Current local**: `v2026.7.7.2-236-gd37090ac3` (236 commits past v2026.7.7.2)
- **Latest tag**: `v2026.7.7.2` (Jul 7, 2026)
- **Behind origin/main**: 1113 commits (per `hermes update --check`)
- **Update available**: YES

### Tags found
- v2026.7.7.2 — same-day patch on v0.18.1, WhatsApp Baileys fix
- v2026.7.7 — v0.18.1 release
- v2026.7.1 — v0.18.0 "The Judgment Release" (~1,720 commits, zero P0/P1)

### Commits since last check (2 days ago): 294 total
- **fix**: ~150 — heavy fix volume (gateway, telegram, mcp, desktop, install/cli, computer_use)
- **other** (merge/test/perf/fmt/chore): ~92
- **refactor**: ~6
- **feat**: ~18 notable features

### User-facing highlights since last check:
**Features:**
- `feat: /model --once` one-turn model override (CLI)
- `feat(codex)`: live app-server events to TUI/desktop, webSearch bubbles, bare hermes-tools names in bridge
- `feat(cache)`: persist exact API bytes in api_content sidecar
- `feat(gateway)`: per-tool status line on Slack, configurable working-state text
- `feat(computer_use)`: cua-driver verify→escalate ladder
- `feat(dashboard)`: schema override for browser.headed toggle
- `feat(tui+cli)`: terminal billing UX (/subscription, /topup)

**Fixes (notable):**
- `fix(agent)`: persist delivered response for tool-call tail rows; request-local Anthropic clients to prevent SQLite corruption
- `fix(telegram)`: cause-agnostic wedged-recovery watchdog + bounded polling drain (#66377) — reconnect freeze fixes
- `fix(gateway)`: dedupe voice transcript echoes, preserve pending voice semantics, invalidate STT cache
- `fix(mcp)`: propagate stored timeouts from completed futures
- `fix(computer_use)`: reconnect dead cua-driver session instead of hanging
- `fix(install)`: keep install.ps1 pure ASCII for Windows PS 5.1
- `fix(cli)`: UTF-16/32 .env handling, harden /model --once

**Performance:**
- Gateway: batch sidebar session slices to single profile-DB pass (desktop)
- Desktop: incremental markdown lexing (~14x less CPU)
- Agent: session.resume model + display history from one SELECT

### Web/docs search
- Web search tools unavailable (FIRECRAWL_API_KEY not set)
- GitHub releases checked via browser — latest tag confirmed v2026.7.7.2
- YouTube/X searches failed due to web_search unconfigured

### Recommendation
Major update needed (1113 commits behind origin/main). The gap includes v0.18.0 "Judgment Release" with massive feature additions and 496 P0/P1 items resolved, plus 18 new features since last check. Breaking changes unlikely for normal users but worth reviewing release notes before updating. Telegram reconnect watchdog fixes are especially important if Jet uses Telegram gateways.
