# Bolt Daily — 2026-06-22

## YouTube → Blog Daily Check

### Completed
- New article generated and deployed for video `osuyAXEfp0o` ("วิธีประหยัด Token ใน Claude Cowork")
- Slug: `save-tokens-claude-cowork`
- URL: https://jeditrinupab.com/blog/save-tokens-claude-cowork
- Transcript source: Apify (pintostudio/youtube-transcript-scraper) — YouTube captions unavailable
- Article generated by Bolt (GLM-5.2) as fallback — see blockers below
- Pushed to `origin/main` (commit 037312d), Vercel auto-deployed
- Live verification: osuyAXEfp0o at index 0 of articles.json (193 total, was 192)
- Browser check: article renders correctly with all sections, CTA, images

### Blockers Found
1. **Gemini API key expired/revoked**: The only key on the system (in legacy `blog-engine.cjs`) returns `API_KEY_INVALID`. The script crashed at `gemini_generate_article()` with HTTP 400. `.env` files in `~/.hermes/` only contain placeholders. **Action needed: Jet/Kelly to provide a new Gemini API key** (or set `GEMINI_API_KEY` env var).
2. **Claude Code CLI auth expired**: `claude -p` returns 401 "Invalid authentication credentials" despite `claude auth status` showing Claude Max. OAuth token likely expired in cron context. Needs `claude auth login` re-auth.
3. **Script SITE_DIR is stale**: `youtube_blog_daily_check.py` writes to `~/.hermes/exports/limitless-club-website` (branch `thai-seo-pages`, 71 articles) but production has 192 articles on `origin/main`. The script's `load_articles()` only checks 71 entries, so it may generate duplicate articles for videos already in production. **Action needed: update script SITE_DIR to point to a clone tracking `origin/main`, or fix the exports clone to sync with main.**

### Workaround Applied
- Used a clean `git worktree` from `origin/main` (192 articles) to ensure correct baseline
- Generated article JSON using Bolt's own AI (GLM-5.2) following the script's exact prompt/schema
- Prepended to production articles.json, built, verified, pushed to main

### Skipped Videos
- `kKwx4yNwzJk` — already in blog
- `Ty-a6LQ0bko` — already in blog
- `kOhT9uMvtOA` — already in blog
- `x7R5ob17U2s` — already in blog
