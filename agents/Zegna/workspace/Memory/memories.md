# Zegna Agent — Durable Memories

## Notion Integration (updated 2025-12-18)
- **Work Output DB ID**: `3581290f50f44b7dbc8b93879ca31916`  
  → Use `database_id` parent for new pages. Title format: `"YYYY-MM-DD Daily Zegna Curation Refresh"`
- **Properties to set on each curation entry**: `Type = 🏷 Curation`, `Status = ✅ Done`, `Agent = Zegna`
- Notion API token stored at: `~/.config/notion/api_key` (token starts with `ntn_55...`)  
  → Verified valid as of 2025-12-18; use POST `/v1/pages` with Content-Type and Notion-Version headers.

## Zegna Curations Setup
- Local curation landing page: `zegna-curations/index.html` — generated by Python script in `zegn...`  
  → Run `update_curations.py` to refresh contents (pulls local file list, generates HTML index)
- Daily cron notes go under `zguna-curations/daily/`  
  → Format: one .md per day summarizing run + any items created in Notion

## Cron Jobs Relevant to Zegna
| Cron Name | Schedule | Notes |
|-----------|----------|-------|
| Zegna daily curation refresh | Daily 07...
