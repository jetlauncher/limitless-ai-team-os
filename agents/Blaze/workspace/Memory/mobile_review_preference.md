# Jet mobile review preference

Jet is usually outside/mobile when asking to review posts, dashboards, or scheduled content. Default to sending the current public tunnel/mobile-access link, not localhost. For Mission Control approvals, verify the public tunnel first and send the `/approvals` link with passcode/login guidance rather than exposing the private key.
