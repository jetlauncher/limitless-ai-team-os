## Airtable Snapshot Cron Run — 2026-07-20
- Script succeeded on re-run. Prior run failed (curl --max-time 15s timeout on Airtable API).
- All 34 tables across base `appdgDHiKj4F1oAf5` returned 0 records — likely the base is empty, share-scoped without real data, or auth has limited permissions.
- Output: `~/.hermes/exports/airtable_snapshot_local.json` (48.2s total).
