# 2026-06-17 — Daily Notes

## Cron: Notion → Obsidian sync fix
- **Issue**: `sync_notion_to_obsidian.py` timed out at 120s on a previous run.
- **Fix**: Re-ran with 180s timeout — completed successfully (30 pages checked, 30 files written, exit code 0).
- **Status**: Transient timeout; resolved on retry. No data loss.


## 2026-06-17 22:32  — Kelly main Telegram stability repair
- Root cause: profile `muse` had the same `TELEGRAM_BOT_TOKEN` as default/Kelly, creating Telegram `getUpdates` polling conflicts that can make the main Telegram channel appear to drop.
- Fix: backed up `~/.hermes/profiles/muse/.env`, disabled only the duplicate `TELEGRAM_BOT_TOKEN`, and restarted/stopped the muse launchd gateway so it no longer polls the main bot. Default/Kelly gateway was not restarted.
- Hardening: added script `~/.hermes/scripts/telegram_gateway_watchdog.py` and cron `kelly-telegram-gateway-watchdog` (`59a0a775cf60`) every 5m; silent when healthy, repairs/reports if default gateway is down or another profile reuses the default token.
- Verification: duplicate Telegram token count is 0; default gateway PID 57550 still running; Telegram Bot API `getMe` and `sendChatAction` succeeded for @Hermeskellyaijedibot; gateway_state says Telegram connected. Also reindexed `~/.hermes/kanban.db` after integrity errors; final integrity check ok.
- Clarified Gmail status after Jet asked: Gmail token files for Work/Personal are present and refresh worked via two_account_gmail_inbox_zero.py (silent = no important items); prior audit invalid_grant was Google Search Console, not Gmail. Separate important_email_filter.py still references missing google-workspace skill path and needs cleanup.
- Applied safe agent-fleet prune: backed up/reindexed ~/.hermes/kanban.db (integrity ok); paused broken jobs GSC weekly, YouTube daily, Bolt Jedi watchdog, UncleChris blocked portfolio check, UncleChris Thai monitor missing pandas. Left profile deletion/API-server ownership/stale plist restarts for explicit follow-up. Full report: Daily/2026-06-17-agent-fleet-prune.md.
- Jet clarified GSC visibility weekly, YouTube daily report, and Bolt Jedi website watchdog are important; marked as keep/fix, not prune. Investigation: YouTube upload token separate from Gmail is failing; GSC token path exists and needs reauth; Bolt watchdog currently completes in ~114s manually but cron 120s cap is too tight.
- Began fixing important crons: optimized Bolt YouTube link audit concurrency; Jedi website watchdog manual runtime improved to ~93s and cron resumed. Generated fresh GSC and YouTube OAuth URLs under /tmp/hermes-oauth-fix/ awaiting Jet redirect URLs to exchange tokens and resume GSC/YouTube report jobs.
