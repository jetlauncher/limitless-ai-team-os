# 2026-07-10 — Daily Notes

## Airtable Snapshot (21:28)
- Script ran successfully.
- Found **54 Airtable bases**; exported data for **3 bases** with **10 tables**, **38 records**.
- Output saved to `~/.hermes/exports/airtable_snapshot_local.json`.

## Evening Shutdown briefing cron — 2026-07-10 21:30 BKK
- Read trinupab@creatuscorp.net calendar for today/tomorrow via Google Calendar API token refresh from ~/.hermes/google_token.json.
- Revenue script returned today THB 19,800 (1 txn), MTD THB 375,357.04 (17 txns).
- Delivered concise Telegram-style shutdown brief; no config changes.

## Hermes session-store cleanup — 2026-07-10 22:37 BKK
- Cleared inactive session history across default + 12 profiles (`blaze`, `bolt`, `jekjack`, `kaijeaw`, `oracle`, `pixel`, `protocol`, `qwen`, `signal`, `tiff`, `unclechris`, `zegna`).
- Policy: keep current/recent user sessions from last 7 days; keep latest 3 runs per cron job; remove old CLI/Telegram/test/API/cron session rows and matching JSON transcripts.
- Result: deleted 38,643 sessions and 445,309 messages; optimized all `state.db` files. Largest DB reductions: default 1.94GB→820MB, oracle 1.71GB→32MB, signal 1.83GB→61MB.
- Verification: integrity check OK for every profile; 0 orphan messages / compression locks. Backups saved under each profile's `backups/` directory before mutation.

## Monthly Hermes session-prune cron — 2026-07-10 22:53 BKK
- Installed durable script `~/.hermes/scripts/hermes_workspace_session_prune.py` for default + profile session cleanup.
- Created active no-agent cron `ffd808b397b5` / `monthly-hermes-workspace-session-prune`, schedule `0 4 1 * *`, deliver `local`, next run `2026-08-01T04:00:00+07:00`.
- Policy: keep recent 7-day user sessions and latest 3 cron runs/job; back up each `state.db`; optimize SQLite/FTS; script stays silent when nothing changes and prints JSON only on dry-run/deletion/error.
- Verified with `py_compile`, dry-run, and `hermes cron list --all`.
