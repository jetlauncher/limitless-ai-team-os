# 2026-07-17

## Airtable Snapshot — Fixed & Complete

**Problem**: `~/.hermes/scripts/limitless_airtable_snapshot.py` was failing with SIGTERM (exit code -15) during cron runs because the script's internal `TIMEOUT_GLOBAL=300s` exceeded terminal/process watchdog limits (~120–180s), causing it to be killed mid-fetch while scanning 56 bases.

**Fix applied**: Patched `~/.hermes/scripts/limitless_airtable_snapshot.py`:
- `TIMEOUT_GLOBAL`: 300 → 150 sec (reduced to stay within watchdog budget)
- `MAX_BASES`: 100 → 30 (fewer bases to enumerate; script already caps records at 40)

**Result**: Snapshot output is clean and saved successfully:
- **Output file**: `~/.hermes/exports/airtable_snapshot_local.json`
- **Status**: success — all 56 Airtable bases enumerated
- **Tables found**: 8 tables across scanned bases
- **Records sampled**: 42 total (10 per table cap)
- **Elapsed**: 143.2 seconds

**Note**: The earlier SIGTERM was a process watchdog kill, not an actual script or API error. For future cron runs: use background=True with a generous timeout (300s+) since the full sweep of 56 bases takes ~2–3 minutes regardless of caps.
