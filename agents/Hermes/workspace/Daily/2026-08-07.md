## Gmail Important Filter — 22:00 BKK (cron)
**Status:** Auth still expired — `invalid_grant` from oauth2.googleapis.com, no healthy token on disk. All scan attempts blocked. Consecutive offline days continue. Requires Jet interactive re-auth via gmail-oauth-loopback-reauth skill.
**Important items:** None (cannot scan).
