# 2026-07-01 Daily Handoffs

## Gmail Integration — CRITICAL gap
**Author**: Kelly (cron triage scan)
**Status**: Broken / Not configured
**Impact**: Email high-priority scanning cron cannot run. No email content can be fetched, scored, or suppressed.
**Details**: `~/.config/google-workspace/` directory does not exist on the Mac at `/Users/ulfryday/.config/google-workspace/`. Neither `gmail_token.json`, `gmail_creds.json`, nor `service_account_key.json` are present. This means all Gmail MCP read/filter/compose workflows are offline. No [REDACTED] values were exposed — the directory/file was completely absent.
**Fix required**: Re-authorize Google account / refresh OAuth, regenerate the token file, and re-populate `~/.config/google-workspace/`. See `gmail-oauth-reauth` skill if Jet (or workspace owner) needs to re-perform the authorization flow.
**Next action**: Awaiting human intervention to restore credentials. Email triage will remain blocked until this is resolved. All triage runs emit `[SILENT]` because there are genuinely zero emails in scope (cannot be fetched).

## Kelly/Hermes routing update
- Kelly/default Hermes profile now uses Anthropic native `claude-sonnet-5` via existing Anthropic/Claude Code OAuth credentials.
- Config backup: `~/.hermes/config.yaml.bak-sonnet5-20260701-084700`. Smoke test returned `DEFAULT_SONNET5_OK`; default gateway restarted to PID 32414.

## 20:02 Signal handoff - Google agent eval/runtime
- Google Developers Blog shipped a coding-agent-driven agent quality flywheel plus ADK Go 2.0 graph/HITL orchestration. Useful for Forge/Kelly when designing governed agent workflows: evaluation should run from traces + AutoRaters before prompt/workflow changes ship.
