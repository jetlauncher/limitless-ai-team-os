# Shared Memory — 2026-06-26

## Hermes Team Kanban upgraded
- Kelly upgraded `~/Projects/hermes-team-kanban` from static localStorage demo to shared agent-native Vercel app at https://hermes-team-kanban.vercel.app.
- APIs: `GET /api/board`, `POST /api/login`, `POST /api/board` actions (`createCard`, `moveCard`, `updateCard`, `deleteCard`, `addComment`, `resetBoard`).
- Human logins: Jet and team; agent logins: Kelly, Blaze, Signal, Bolt, Qwen. See repo `README.md` / `AGENTS.md` for non-secret operation details.
- Agent API key is local only at `~/Projects/hermes-team-kanban/.private/agent_api_key.txt`; never print/expose it. Use it as `X-Agent-Key` when agents need to create/move/comment on tasks.
- Datastore: private GitHub Gist configured through Vercel production env. Verified production smoke: board read, team login, agent create/delete card, browser login.
- Public student repo now live: https://github.com/jetlauncher/hermes-team-kanban (branch `main`, commit `99027c5`). Includes full sanitized source plus student build prompt PDF at `docs/Hermes-Team-Kanban-Student-Build-Prompt.pdf`; secret scan passed before push.

## 21:04 ICT Signal daily AI intel handoff
- Signal daily report script completed successfully for 2026-06-26. Telegram final curated from official/source-rich items rather than stale GitHub star-only rankings.
